const nome = 'felipe'

const conca = `oi ` + nome + ` !`

const template = `
                ola 
                ${nome}!`

console.log(conca,template)

const up = texto => texto.toUpperCase();

console.log(`Ei... ${up('cuidado')}!`);